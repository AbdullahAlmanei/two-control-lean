import TwoControl.CosineSine.Statements

/-!
This file exists only to give the cosine-sine appendix work its own build target.

Build this module directly with:

`lake build TwoControl.CosineSine.Main`

so the new formalization can evolve without forcing a full rebuild of the section-driven proof
development.
-/
